PORT=3333
NODE_ENV=development

# Origens permitidas no CORS (separadas por vírgula)
CORS_ORIGIN=http://10.21.1.51:3000,http://localhost:3000,http://127.0.0.1:3000

PORT=3333
NODE_ENV=development

# Origens permitidas no CORS (separadas por vírgula)
CORS_ORIGIN=http://10.21.1.51:3000,http://localhost:3000,http://127.0.0.1:3000

# Desenvolvimento: use DB_HOST/DB_PORT/DB_USER/DB_PASSWORD/DB_NAME
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=frik
DB_SSL=false

# Produção (Vercel/Aiven): use DATABASE_URL em vez das variáveis acima
# DATABASE_URL=mysql://usuario:senha@host:porta/database?ssl-mode=REQUIRED

JWT_SECRET=dev-secret-change-me-in-production
JWT_EXPIRES_IN=7d

TAXA_TROCA_PONTOS=50
PONTOS_POR_REAL=10

TAXA_TROCA_PONTOS=50
PONTOS_POR_REAL=10
